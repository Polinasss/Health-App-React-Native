import React, { useState } from "react";
import { Text, View, SafeAreaView, Pressable, Modal, Dimensions, TouchableWithoutFeedback } from "react-native";

interface IProps {
}

export const BottomPopUp: React.FC = () => {
    const [show, setShow] = useState(false);

  return (
    <View></View>
  );
};
